server:  port: 3000spring:  application:    name: my-gateway  cloud:    nacos:      discovery:        server-addr: localhost:8848        metadata:          group: chl-group      config:        enabled: true        server-addr: localhost:8848        file-extension: yaml        refresh-enabled: true        extension-configs:          - data-id: gray-config.yaml            group: DEFAULT_GROUP            refresh: true